# Local dev: leave UNSET to use the Vite proxy ('/api' → http://localhost:8000).
# Production (Vercel): set this to your Koyeb backend, including the /api suffix:
#   VITE_API_BASE_URL=https://your-app.koyeb.app/api
VITE_API_BASE_URL=

// seed removed — all data now comes from the API (see src/api/client.ts).
// bandHours is UI grid configuration (the calendar's time bands), not seed data.
export const bandHours = [8, 10, 12, 14, 16, 18, 20];
